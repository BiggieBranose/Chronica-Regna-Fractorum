# Project Instructions

## Git
- Do NOT run any git commands (add, commit, push, reset, etc.). The user handles all git operations themselves.
- Never stage files or create commits unless the user explicitly and unambiguously asks you to do so.
